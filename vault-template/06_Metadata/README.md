# Metadata

This folder holds seed notes and conventions that keep the vault reusable.

## Project planning layout

The preferred project layout is:

- `02_Projects/<ProjectName>/GSD/`
- `.planning` as a symlink to that directory
- `.planning` excluded locally via `.git/info/exclude`

See `Plans/GSD-project-bootstrap.md` for the bootstrap note.
